#pragma once
#include "parse.h"
#include <unordered_map>
#include <map>
#include <vector>
#include <string>
using namespace std;

//comment this later
void shellSort(std::vector<std::pair<std::string, int>>& data);
void shellSort(std::vector<std::pair<int, int>>& data);